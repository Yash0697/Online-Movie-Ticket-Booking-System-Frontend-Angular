import { FilterShowsPipe } from './filter-shows.pipe';

describe('FilterShowsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterShowsPipe();
    expect(pipe).toBeTruthy();
  });
});
